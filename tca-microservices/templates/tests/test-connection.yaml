apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tca-microservices.fullname" . }}-test-connection"
  labels:
    {{- include "tca-microservices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tca-microservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
