apiVersion: v1
kind: Service
metadata:
  name: {{ include "tca-microservices.fullname" . }}
  labels:
    {{- include "tca-microservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tca-microservices.selectorLabels" . | nindent 4 }}
